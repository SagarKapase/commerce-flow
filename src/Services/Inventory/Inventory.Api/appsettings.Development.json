{
  "Jwt": {
    "SigningKey": "dev-only-signing-key-change-me-32chars-minimum-commerceflow"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",

      // Leave this on while working through Phase 6. The UPDATE statements it
      // prints are the whole point: you can see "AND Version = @p" in the
      // WHERE clause, which is the optimistic concurrency check itself.
      "Microsoft.EntityFrameworkCore.Database.Command": "Information",
      "Microsoft.AspNetCore.Authentication": "Information"
    }
  }
}
